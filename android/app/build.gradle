plugins { id 'com.android.application' }
android {
    namespace 'one.aurion.app'
    compileSdk 35
    defaultConfig {
        applicationId 'one.aurion.mobile.superstudio'
        minSdk 26
        targetSdk 35
        versionCode 61
        versionName '6.1.0-zero'
        manifestPlaceholders = [appLabel: 'AURION ONE ZERO']
    }
}
dependencies { implementation 'androidx.documentfile:documentfile:1.0.1' }
