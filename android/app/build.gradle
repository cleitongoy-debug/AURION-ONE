plugins { id 'com.android.application' }
android {
    namespace 'one.aurion.app'
    compileSdk 35
    defaultConfig {
        applicationId 'one.aurion.mobile.studio'
        minSdk 26
        targetSdk 35
        versionCode 40
        versionName '4.0.0'
        manifestPlaceholders = [appLabel: 'AURION ONE STUDIO']
    }
}
dependencies {
    implementation 'androidx.documentfile:documentfile:1.0.1'
}
