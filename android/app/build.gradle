plugins { id 'com.android.application' }
android {
    namespace 'one.aurion.app'
    compileSdk 35
    defaultConfig {
        applicationId 'one.aurion.mobile.fixed'
        minSdk 26
        targetSdk 35
        versionCode 20
        versionName '2.0.0'
        manifestPlaceholders = [appLabel: 'AURION ONE MOBILE FIXO']
    }
}
