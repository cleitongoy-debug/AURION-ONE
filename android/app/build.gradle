plugins { id 'com.android.application' }
android {
    namespace 'one.aurion.app'
    compileSdk 35
    defaultConfig {
        applicationId 'one.aurion.mobile.superstudio'
        minSdk 26
        targetSdk 35
        versionCode 58
        versionName '5.8.0'
        manifestPlaceholders = [appLabel: 'AURION SUPER STUDIO']
    }
}
dependencies {
    implementation 'androidx.documentfile:documentfile:1.0.1'
}
