plugins { id 'com.android.application' }
android {
    namespace 'one.aurion.app'
    compileSdk 35
    defaultConfig {
        applicationId 'one.aurion.mobile.superstudio'
        minSdk 26
        targetSdk 35
        versionCode 60
        versionName '6.0.0'
        manifestPlaceholders = [appLabel: 'NOVO AURION FINAL PART']
    }
}
dependencies {
    implementation 'androidx.documentfile:documentfile:1.0.1'
}
