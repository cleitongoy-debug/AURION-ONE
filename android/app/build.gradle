plugins { id 'com.android.application' }
android {
    namespace 'one.aurion.app'
    compileSdk 35
    defaultConfig {
        applicationId 'one.aurion.mobile.final'
        minSdk 26
        targetSdk 35
        versionCode 30
        versionName '3.0.0'
        manifestPlaceholders = [appLabel: 'AURION ONE MOBILE FINAL']
    }
}
