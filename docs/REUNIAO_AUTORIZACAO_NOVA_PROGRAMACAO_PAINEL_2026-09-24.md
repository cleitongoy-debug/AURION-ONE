# AURION ONE — REUNIÃO DE AUTORIZAÇÃO DA NOVA PROGRAMAÇÃO DO PAINEL

**Data:** 24/09/2026  
**Operador:** Cleiton Luiz Epifanio / ANARK  
**Repositório oficial:** `cleitongoy-debug/AURION-ONE`  
**Drive de referência:** `1W-4pD2hKnEUenNGVr2N_ZdNwxGDkYaaA`  
**Estado:** AUTORIZAÇÃO OPERACIONAL ATIVA PARA DESENVOLVIMENTO CONTROLADO

## 1. Ordem do operador

O outro agente está autorizado a estudar os materiais disponíveis no Google Drive e no repositório AURION-ONE e a montar a nova programação do painel, incluindo PC e celular, desde que preserve integralmente o painel estável e siga esta ata.

A autorização permite:

- ler a Bíblia, reuniões, laboratório, documentação e código atual;
- mapear a arquitetura real antes de alterar arquivos;
- criar módulos, dependências, testes, documentação e interfaces;
- criar uma branch de desenvolvimento própria, commits verificáveis e Pull Request;
- corrigir abas existentes e acrescentar as funções solicitadas;
- preparar instaladores e inicializadores reversíveis;
- produzir diagnóstico real, logs e evidências de funcionamento.

Esta autorização **não permite merge direto no `main` nem substituição do painel estável sem validação e aprovação final do operador**.

## 2. Leitura obrigatória antes de programar

O agente deve ler, nesta ordem:

1. `README.md`;
2. `LABORATORIO_IA.md`;
3. `Biblia_da_Inteligencia_Artificial_AURION_ONE.docx`;
4. `docs/REUNIAO_OFICIAL_IA.md`;
5. `docs/REUNIAO_OPERACIONAL_2026-09-19.md`;
6. `docs/REUNIAO_OPERACIONAL_2026-09-21_EVOLUCAO_PAINEL.md`;
7. `docs/AUDITORIA_BIBLIA_REUNIAO_PAINEL_2026-09-19.md`;
8. atas de Android, POCO, T8i, módulos visuais e continuidade;
9. `AURION_ONE.py`, `ADAPTA.py`, `START_AURION.cmd`, `remote-agent/`, `android-app/`, `android/` e `mobile/`;
10. inventário do Drive indicado nesta ata.

Não reconstruir o sistema a partir de uma descrição genérica. A base real precisa ser identificada primeiro.

## 3. Inventário confirmado do Drive

Na pasta compartilhada foram encontrados:

- `OCTANE TENTATIVA 1`;
- `OCTANE TENTATIVA 2`;
- `OCTANE TENTATIVA 3`;
- `OCTANE 2022.1.rar`, aproximadamente 2 GB;
- documentação de instruções e problemas;
- pastas de plugin, `thirdparty` e inicialização;
- arquivos de credenciais/licença e componentes identificados como crack.

Esses materiais são referência de diagnóstico, não autorização para executar binários desconhecidos.

## 4. Limites de segurança e integridade

- Não executar, distribuir, versionar ou integrar cracks, `YellowStar`, licenças alteradas, credenciais copiadas ou mecanismos de desvio de ativação.
- Não enviar ao GitHub tokens, senhas, cookies, OAuth secrets, chaves, arquivos de licença ou dados privados. O repositório é público.
- Não colocar o arquivo RAR, modelos grandes, fotos, vídeos ou binários no Git.
- Não apagar arquivos do PC, Drive ou repositório. Toda limpeza deve ser substituída por backup e movimentação reversível.
- Não sobrescrever `AURION_ONE.py`, `ADAPTA.py`, inicializadores ou bases travadas antes de comparar versões e criar rollback.
- Não expor portas do PC diretamente à internet. Usar autenticação e rede privada.
- Integrações Google, GitHub e provedores de IA devem usar OAuth/API oficial; sessões pagas de aplicativos não equivalem a API.
- Estados ON/OFF precisam vir de verificações reais com horário, nunca de simulação visual.

## 5. Branch e método autorizados

Branch recomendada:

```text
feat/painel-integrado-vnext
```

Fluxo obrigatório:

1. registrar commit-base e arquivos que iniciam o painel real;
2. criar backup e mapa de rollback;
3. desenvolver por módulos, evitando arquivo monolítico;
4. manter contratos compatíveis com o painel existente;
5. testar cada módulo isoladamente;
6. testar inicialização completa no Windows;
7. testar interface móvel/APK separadamente;
8. registrar logs, versões, dependências e evidências;
9. abrir Pull Request, sem merge automático;
10. aguardar validação final do operador.

## 6. Escopo funcional autorizado

### Núcleo e continuidade

- painel adaptável para desktop e celular;
- agente local com chat, memória persistente autorizada e histórico exportável;
- escolha de pastas no PC/celular, depósitos de conversas e salvamento imediato;
- backup, integridade, recuperação, migração e rollback;
- scanner de dependências, versões, portas, serviços, modelos e armazenamento;
- logs com distinção entre demonstrado, implementado, planejado e bloqueado.

### Estúdio criativo independente

- laboratório de foto, vídeo e áudio dentro do AURION, sem depender apenas de atalhos externos;
- edição não destrutiva, presets, LUTs, cor, exposição, contraste, curvas, máscaras e comparação antes/depois;
- conversão de formatos com fila, progresso, metadados, validação e saída escolhida pelo operador;
- integração opcional com FFmpeg, ImageMagick e bibliotecas legais instaladas de forma isolada;
- IA integrada aos fluxos com seleção explícita de modelo/provedor e indicação real de disponibilidade;
- laboratório de mistura, testes A/B, parâmetros reproduzíveis e promoção somente após validação.

### Canon T8i / CR3

- aba T8i integrada ao painel real;
- importação por cópia, nunca mover o original;
- workspace escolhido pelo operador;
- `RAW`, `PREVIEWS`, `EXPORTS`, `CONVERSAS`, `PRESETS`, `LOGS` e backups;
- revelação CR3, preview, ajustes, presets e exportações JPEG/PNG/TIFF quando suportados;
- sidecar JSON com origem, horário, versão e parâmetros;
- conversas e memória específicas do projeto fotográfico;
- diagnóstico das dependências sem impedir a abertura do painel principal.

### Imagem, vídeo, áudio e geração

- inventário de modelos, LoRAs, ControlNet, workflows e nós;
- ComfyUI/Ollama com status obtido das APIs reais;
- prompts positivo/negativo, referências, seed, filas, progresso, preview e salvamento;
- funções inspiradas em fluxos profissionais de tratamento, montagem, composição, motion, áudio e conversão, implementadas de forma própria ou por dependências licenciadas/open source;
- não copiar código, marcas ou interfaces proprietárias de CapCut, Canva, Photoshop, After Effects, DaVinci Resolve ou similares.

### Contas e conexões

- configuração de Google Drive, GitHub e outros serviços por mecanismos oficiais;
- cofre local/seguro para configurações sensíveis, fora do Git;
- tela de conexão, desconexão, teste, permissões e última verificação;
- nenhum login deve ser declarado funcional sem teste autorizado.

### POCO X7, APK e Mi Band 9 Pro

- APK responsivo com as funções compatíveis do painel;
- operação local no celular sempre que tecnicamente possível;
- comunicação autenticada com o PC quando ele estiver disponível;
- integração com Android/Mi Fitness limitada às APIs e permissões oficialmente disponíveis;
- não prometer leitura ou controle da Mi Band sem prova técnica;
- build reproduzível, assinatura identificada, versão, changelog e artefato testável.

### Cinema 4D e Octane

- aba de diagnóstico de versões, plugins, GPU, drivers, logs, cenas e render;
- integração apenas com plugin/licença oficial e versões compatíveis;
- nenhuma automação de crack, alteração de licença ou execução de binário desconhecido;
- testes devem preservar cenas originais e gerar cópias de saída.

## 7. Critérios mínimos de aceitação

- O painel estável continua abrindo pelo inicializador atual.
- A nova versão abre por inicializador próprio e reversível.
- Nenhum arquivo do usuário é apagado ou sobrescrito silenciosamente.
- Cada botão importante possui ação real, estado de erro e log.
- Pastas e locais de salvamento são escolhidos pelo operador.
- Conversas, memórias, presets e projetos persistem após reiniciar.
- Dependências são instaladas em ambientes isolados quando possível.
- PC e APK apresentam versões e contratos compatíveis.
- Recursos indisponíveis aparecem como indisponíveis, não como funcionais.
- Testes automatizados e roteiro manual cobrem inicialização, salvamento, restauração e integração.
- O Pull Request informa arquivos alterados, riscos, testes, evidências e rollback.

## 8. Entregas exigidas do outro agente

1. relatório do estado atual e ponto real de inicialização;
2. mapa dos módulos existentes e lacunas;
3. plano incremental com dependências;
4. implementação em branch própria;
5. testes e evidências;
6. pacote PC reversível;
7. APK versionado e reproduzível, quando o código estiver validado;
8. documentação de instalação, uso, backup e recuperação;
9. Pull Request para revisão do operador;
10. atualização do `LABORATORIO_IA.md` com resultados reais.

## 9. Regra final

O objetivo é evoluir o painel real já existente. O agente está autorizado a programar e propor correções, mas não a declarar conclusão sem testes, não a apagar a base estável e não a fundir alterações no `main` sem aprovação final do operador.

**Assinatura operacional registrada por solicitação de Cleiton Luiz Epifanio em 24/09/2026.**
